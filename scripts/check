#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

"$ROOT/scripts/lint"
python3 -m unittest discover -s "$ROOT/tests/structural" -p 'test_*.py' -v

echo "dut-tools check: PASS"
